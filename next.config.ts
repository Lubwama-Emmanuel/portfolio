import type { NextConfig } from "next";

/**
 * Canonical URL for Open Graph / Twitter is driven by `NEXT_PUBLIC_SITE_URL`
 * (see `.env.example`) and defaults in `src/lib/site.ts` — not set here.
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
